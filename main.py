from ipobot import MeroShare
import json

APPLIED_KITTA = 10


meroshare = MeroShare()

# meroshare.login(data.DP,data.Username, data.Password)
# meroshare.find_ipo()
# meroshare.apply_ipo(APPLIED_KITTA, data.crn)
# meroshare.enter_pin(data.pin)
# meroshare.logout()